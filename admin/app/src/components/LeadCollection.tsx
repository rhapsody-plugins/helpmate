/** Stub for admin — Coupon imports this but never renders when skipLeadGate is true */
export default function LeadCollection(_props: {
  setCollectLead?: (collectLead: boolean) => void;
  title?: string;
  variant?: 'small' | 'large';
  textColor?: string;
}) {
  return null;
}
